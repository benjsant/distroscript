# Delta propre à cet environnement.
# Les paquets communs sont dans lib/packages_common.txt.
#
# Les douze -dev qui figuraient ici (libssl-dev, libbz2-dev, tk-dev,
# libxmlsec1-dev…) n'existaient que pour permettre à pyenv de compiler CPython
# depuis les sources. uv télécharge des binaires précompilés, ils sont devenus
# inutiles.

openssh-client
lsof
strace

# Quelques -dev restent utiles pour compiler des wheels natives
libssl-dev
libffi-dev

# Python dev
httpie
sqlite3
